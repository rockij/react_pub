# Netlify 배포 설정 - Storybook

[build]
  command = "npm run build-storybook"
  publish = "storybook-static"

# SPA 라우팅을 위한 리다이렉트 설정 (Storybook 내부 라우팅 지원)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

